/**
 * Server-side re-export of auth config.
 * Handlers import from here so the path stays relative to src/server/.
 */
export { BUSINESS_AUTH, ADMIN_AUTH, APP_CONFIG } from '../../lib/auth-config';
